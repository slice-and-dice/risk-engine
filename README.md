# Risk Engine

## `/calculate`
* `POST` method.
* Use the following JSON body format with the `POST` request:
  ```
  {
    "answerIds": ["TYPE-001", "TYPE-002", "003", "004", "005", "etc..."]
  }
  ```